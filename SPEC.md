# SPEC —— 引用规范

> 给谁看：要把自己的课件、题目、教案、学习档案标注到本项目锚点 ID 上的**外部开发者与教研机构**。
>
> 只回答一件事：**你今天写下的那个 `ca_XXXXXXXX`，下次我们更新之后还算不算数。**

本文是**对外承诺**。承诺之外的部分（我们内部怎么抽、怎么审、怎么改）在
[README.md](README.md) / [PROVENANCE.md](PROVENANCE.md) / [DECISIONS.md](DECISIONS.md) 里，
那些会变，本文列出的承诺不会 —— 要变必须走 [§11](#11-这份文件本身怎么变) 的流程。

**适用范围**：`release` <!--N:release-->0.1.0<!--/N--> ·
`schemaVersion` <!--N:schemaVersion-->0.1.0<!--/N--> ·
数据生成于 <!--N:generatedAt-->2026-08-24<!--/N-->。

> ⚠️ **待接线**：本文里所有数字都用 `<!--N:键-->…<!--/N-->` 标记从 `manifest.json` 现读，
> 但 `scripts/sync-docs.mjs` 目前只扫 `README.md` 与 `PROVENANCE.md`，**还没扫本文**。
> 在把 `SPEC.md` 加进那份文件列表、并在 `VALUES` 里注册本文用到的新键之前，
> 这里的数字是写入当天的快照值，**不受 CI 核对**。
> 这一段是给维护者看的施工牌，接线完成后删掉它 —— 留着一个「数字会自动更新」的假承诺，
> 正是本仓库栽过跟头的那类错（见 PROVENANCE.md 开头）。

---

## 0. 先读这三个数，再决定要不要用

| | |
|---|---|
| 存活锚点 | <!--N:liveAnchors-->2971<!--/N--> 条 |
| 可被档案引用（`usableAnchors`） | <!--N:usable-->2589<!--/N--> 条 |
| **有教师签字的** | **<!--N:humanConfirmed-->0<!--/N--> 条** |

**第三个数是 0，它没有写错。** 底座里没有任何一条断言经过一线教师复核。
标为「可用」的那些，靠的是机械可判定（字表、词表这类数得清的东西）或 AI 裁定待异议 ——
那是「没人反对」，不是「有人认可」。详见 [§5](#5-复核档可引用这条线画在哪)。

先修边的情况更弱：<!--N:edges-->6622<!--/N--> 条边里
**<!--N:edgesUnreviewed-->6619<!--/N--> 条的 `reviewStatus` 仍是 `llm-proposed`**，
即没有任何人看过。见 [§6](#6-边依赖关系怎么引用)。

拿去做产品之前，请自己判断这个可信度够不够。我们不替你判断，也不打算把这几个数藏起来。

---

## 1. 术语

本文尽量不用内部黑话。下面这几个避不开，先定义：

| 词 | 意思 |
|---|---|
| **锚点**（anchor） | 一条可判定的能力断言，一个稳定 ID。**你要引用的就是它。** |
| **断言**（`statement`） | 锚点的正文。判据只有一条：能不能对一个具体孩子在某一时刻回答「会 / 不会 / 多大程度」。 |
| **边 / 前置**（edge / prerequisite） | 「学 B 之前得先会 A」。边没有自己的 ID，主键是 `(anchorId, prerequisiteId)` 这一对。 |
| **档位**（`track`） | 学科的结构类型：`DAG`（强先修，数理化）／`LIST`（清单覆盖，字表词表篇目）／`MATRIX`（能力维度×主题，史地生政科）。决定这个学科允许存在什么边。 |
| **复核档**（`reviewStatus`） | 这条断言被谁看过、看到什么程度。决定它能不能被引用。 |
| **可引用**（citable） | 允许被个人学习档案当主键引用。唯一定义在机器可读的 [`mappings/citable.json`](mappings/citable.json)，不在任何文档里。 |
| **清单**（list） | L1 层的事实性名单：字表、词表、背诵篇目。共 <!--N:listItems-->6262<!--/N--> 条。 |
| **L0–L4** | 分层：L0 锚点 / L1 映射与清单 / L2 教材编排（**不在本仓库**）/ L3 个人档案（**永不进本仓库**）/ L4 应用。 |
| **快照**（release） | 一次对外发布的数据状态，由 `manifest.json` 完整描述。 |

---

## 2. 怎么取数据

站点是纯静态，没有后端、没有鉴权、没有配额，也**没有 SLA**。

```bash
# 入口永远是 manifest：所有计数、每个文件的 SHA-256 都在里面
curl https://k12.yongle.school/data/manifest.json

# 某一科的锚点（JSONL，一行一条 JSON）
curl https://k12.yongle.school/data/anchors/math.jsonl

# 边、清单、映射同理
curl https://k12.yongle.school/data/edges/math.jsonl
curl https://k12.yongle.school/data/lists/hanzi/jiben-300.jsonl
```

浏览器里可直接 `fetch`，跨域已放开（`Access-Control-Allow-Origin: *`）。
人读的入口：`/data/`（文件清单）、`/list/`（全部能力点目录）、
`/a/<id>/`（每条锚点一个详情页，例如 `/a/ca_YK9SqQvF/`）。
仓库同样是数据源，`git clone` 得到的 `anchors/` `edges/` `lists/` `mappings/` 与线上一致。

### 你的系统里应该存什么

最小引用记录 —— **三样，缺一样都会在下次更新时说不清**：

```json
{
  "anchorId": "ca_YK9SqQvF",
  "substrateRelease": "0.1.0",
  "substrateGeneratedAt": "2026-08-22"
}
```

- `anchorId` 是你和底座之间唯一的约定。**不要存 `statement` 当外键**（它会被改写），
  更不要存文件名或行号（见 §3「不承诺」）。
- 记下取数时的 `release` + `generatedAt`，是为了将来能回答「我这批标注是照着哪一版做的」。
- 想要更硬的证据，再存一份当时的 `manifest.files[<你读的文件>].sha256`。

---

## 3. ID 稳定性承诺

`ca_` + 8 位字母数字。**校验请按正则 `^ca_[A-Za-z0-9]{8}$`** —— 铸造用的字母表确实去掉了
易混的 `0/O/1/l/I`，但那是铸造实现，不是契约，别拿它当校验规则。

### 承诺

1. **永不复用。** 一个 ID 一旦被铸出，就永远只指这一件事。弃用记录、退休记录（`retired/`）
   里的 ID 也算已用，不回收。这条由 `scripts/lib/mint.mjs` / `tools/mint_py.py` 执行：
   铸造前必须加载全部已用 ID。
   > 为什么这么较真：复用一个「看起来没人用」的 ID，会把两个孩子的不同能力记录悄悄合并，
   > 而且没有任何报错。这是这套系统里最贵的错误。
2. **永不改写含义。** 断言文本可以被修饰（错别字、标点、表述打磨），ID 不变；
   但**含义变了就不是改写** —— 那时的做法是弃用旧锚点、铸一个新 ID，见 [§4](#4-弃用与继任)。
   改写过的锚点会保留 `statementBefore`（改写前的原句），让你能自己判断这次改动算不算改含义。
3. **永不删除。** 锚点只会被标 `deprecated: true`，仍然留在数据文件里、仍然能被查到。
   一个 ID 从数据里**彻底消失**属于违约，请报 issue。
4. **ID 无语义。** 不要试图从 ID 里解析学科、年级、单元、顺序 —— 里面没有这些东西，
   这是故意的（学科会拆分、年级会随教材变、单元是 L2 的事）。

### 不承诺

- **不承诺 ID 的文件归属稳定。** `anchors/math.jsonl` 与 `anchors/rewrite-数学.jsonl`
  这种切分是内部实现，会调整。**按 ID 全局查找，不要按文件建索引。**
- **不承诺行序稳定。** JSONL 的行顺序随重算变化，不表达任何含义。
- **不承诺锚点总数单调增。** 已经掉过一次：清理劣质锚点使
  <!--N:deprecatedAnchors-->1026<!--/N--> 条退出存活集合。累计铸出的 ID 共
  <!--N:anchorsAll-->3997<!--/N--> 个。
- **不承诺 `usableAnchors` 单调增。** 复核可以把一条从可用降级为 `disputed`；
  降级只需一个人说不对（宁可错杀）。

---

## 4. 弃用与继任

### 4.1 三个字段

| 字段 | 含义 |
|---|---|
| `deprecated: true` | 这条锚点退出存活集合。**不再可引用**，但记录仍在。 |
| `supersededBy: "ca_…"` | 继任者。有它，说明这条被另一条**替代**了，语义大体延续。 |
| `dropReason: "…"` | 弃用原因（自然语言，必填其一）。没有继任者时它就是唯一的交代。 |

校验器（`scripts/validate.mjs`）强制这四条，你可以当作不变式来写代码：

1. 弃用锚点必须有 `supersededBy` **或** `dropReason`，二选一 —— 否则你的引用会悬空且查不到原因。
2. `supersededBy` 必须指向**存在且存活**的锚点。**不会出现继任链**（A→B，而 B 自己也已弃用）。
3. **不会有边指向已弃用锚点。** 弃用前必须先迁移或退休相关边。
4. 弃用记录是历史，**不重写历史** —— 后加严的 schema 约束不追溯弃用条目。
   所以：解析弃用记录时请放松字段校验，别拿存活锚点的期望去卡它。

### 4.2 实际分布：多数弃用**没有继任者**

当前 <!--N:deprecatedAnchors-->1026<!--/N--> 条弃用锚点里：

- <!--N:supersededAnchors-->12<!--/N--> 条有 `supersededBy`（有继任者）
- <!--N:droppedNoSuccessor-->1014<!--/N--> 条只有 `dropReason`（**没有继任者**）

**这是本节最重要的一句话**：绝大多数弃用不是「换了个更好的说法」，
而是「这条根本不该存在」—— 典型的 `dropReason` 是「AI 判定不是学生能力（教学建议/课程目标）」
「原文是面向教师的表述，主语不是学生」。
强求每条都填 `supersededBy` 只会逼人随便指一个，那才真会让你的档案错乱，所以我们不填。

### 4.3 引用了被弃用锚点，怎么办

```js
const a = anchorsById.get(myRef.anchorId);

if (!a)                      → 违约或你读的文件不全。全量取一遍；仍然没有就报 issue。
if (!a.deprecated)           → 正常，什么都不用做。
if (a.deprecated && a.supersededBy)
                             → 有继任者。见下「怎么迁移」。
if (a.deprecated && !a.supersededBy)
                             → 没有继任者。这条断言在底座里没有落脚点了。
                                见下「没有继任者时」。
```

**有继任者时（迁移）**

1. 把你的标注指向 `supersededBy`，**并保留原 ID 作为历史**（`migratedFrom`）。
   不要原地覆盖 —— 覆盖之后你就再也答不出「这条标注当初是照着哪条做的」。
2. 迁移不是自动等价。继任者的 `statement` 与原条可能有出入，**过一遍人眼**，
   两条的正文都在数据里，对着看即可。
3. 继任者的复核档可能与原条不同，迁移后重新检查它是否 citable。

**没有继任者时**

1. **不要**自己在库里造一个替代锚点。这条铁律一破，底座立刻退化成又一个数据集
   （见 README「一条铁律」）。
2. 先读 `dropReason`。多数情况下它会告诉你：那条根本不是一条学生能力
   （是教学建议、课程目标、指标数字），你标注的那批内容可能本来就该挂在别处。
3. 如果你确信这是一条真实的、可判定的学生能力，走 [§10](#10-要新锚点或发现错误) 的申请通道。
4. 在有结论之前，把你这批标注标为「悬空」，**不要静默丢弃**。
   静默丢弃会让你的历史数据变得无法解释。

### 4.4 复合母条与拆出来的子条（`composite` / `splitFrom` / `component` 边）

课标里有大量复合要求，比如「能区分所听音乐的体裁、类别和表现形式，并运用所学知识分析其特点」。
这种要求**真实存在**（课标自己就是这么印的），但**不能当档案的记录单位** ——
记「会/不会」会出现「半会」，而「半会」不是一个能记录的状态。

所以我们把它拆开，规则如下：

| | |
|---|---|
| 母条 | 标 `composite: true`，**不弃用**、ID 不变、继续存在。 |
| 子条 | 各自是独立锚点、有自己的 ID，`provenance.splitFrom` 指回母条。 |
| 两者的关系 | 一条 `type: "component"` 的边：`{ "anchorId": 母条, "prerequisiteId": 子条 }`，读作**母条依赖子条**（子条是母条的一个子动作）。 |

当前 <!--N:compositeAnchors-->226<!--/N--> 条母条、
<!--N:splitChildren-->548<!--/N--> 条子条、
<!--N:edgesComponent-->3067<!--/N--> 条 `component` 边。

**母条为什么不弃用**：它是课标自己的单位，而且弃用它会让**指向它的现存边全部悬空**，
那些边该改指哪个子条，机械上判不出来（「会读 3000」和「会写 3000」，原来那条边指的是哪个？）。

**给引用方的建议（这是建议，不是承诺）**：

- 要记录一个孩子的掌握状态 → **挂子条**，母条的「会/不会」问不清楚。
- 要对齐课标条目、做覆盖率统计 → 母条更合适。
- 通过 `component` 边可以从母条枚举出它的全部子条，反之用 `provenance.splitFrom`。

---

## 5. 复核档：可引用这条线画在哪

### 5.1 六个取值

`reviewStatus` 是锚点上的 schema 枚举，共六个值。当前分布：

<!--N:reviewTable-->| 状态 | 条数 | 能否被 L3 档案引用 |
|---|---:|---|
| `auto-confirmed` | 146 | 能 |
| `ai-adjudicated` | 242 | 能（AI 裁定，**待人工异议**） |
| `expert-confirmed` | 0 | 能 |
| `ai-reviewed` | 2201 | 能（2026-08-20 起 —— **AI 看过、没挑出毛病**，不是教师签字） |
| `disputed` | 20 | **不能** —— AI 复核挑出了具体问题 |
| `llm-proposed` | 362 | **不能** —— 没有任何东西看过一眼 |
| **存活合计** | **2971** | 其中 **2589** 可用 |<!--/N-->

各档的确切含义：

- `auto-confirmed` —— **判定客观、根本不需要人**（写对/读对/背下来这类数得清的东西）。
  依据记在 `autoConfirmBasis`。
- `expert-confirmed` —— **一线教师当面确认并署名**。凭据是 `endorsements` 数组
  （谁、什么时候、说了什么）。**目前 <!--N:humanConfirmed-->0<!--/N--> 条。**
- `ai-adjudicated` —— AI 带全部材料做过裁定，**待人工异议**。
  `adjudication.pendingObjection = true` 标着它不是定论。
  它成立的前提是「没人反对」，不是「有人认可」。
- `ai-reviewed` —— 过了 AI 学科审查，没被挑出问题。**AI 看过、没挑出毛病，仅此而已。**
- `disputed` —— AI 复核挑出了具体问题（记在 `aiIssues`），或有人标了「学段不对/表述要改/不该收」
  （记在 `disputes`）。挂起，不可引用。
- `llm-proposed` —— 模型提议，**没有任何东西看过一眼**。不可引用。

### 5.2 「可引用」的唯一定义是一个文件，不是这段文字

```
mappings/citable.json  →  citable: ["auto-confirmed", "expert-confirmed",
                                    "ai-adjudicated", "ai-reviewed"]
                          notCitable: { disputed, llm-proposed }
```

**请你的代码也读这个文件，不要把这四个字符串抄进自己的常量表。**
这套集合在本仓库内部曾经散落 8 处、有三种不同取值 —— 同一个概念多份定义，
是这个项目反复栽跟头的那一类错。我们把它收敛成一份，你也别再复制一份出去。

`manifest.json` 里的 `usableAnchors` 就是按这份定义算出来的存活锚点数。

### 5.3 这条线 2026-08-20 被放宽过，说明如下

原本 `ai-reviewed` 不计入可引用，可用锚点只有 388 条（这个 388 是当日的历史数字，
引自 `mappings/citable.json` 的决策记录，不随数据更新）。当日的决策（由项目发起方明示）
把 `ai-reviewed` 纳入，理由记在 `mappings/citable.json` 的 `decision` 字段里：

> 教师签字通道本来就还没开，388 这个数被卡在那里不是因为数据不好，而是因为唯一能抬它的那一步不做。
> 继续把上千条 AI 复核过的锚点标成「不可引用」，等于让底座对外没有可用规模。

同时保留的边界：

- `disputed` 与 `llm-proposed` **仍然不可引用**。放宽的是「AI 看过、没挑出毛病」这一档，不是全部放行。
- **没有伪造任何教师签字。** `expert-confirmed` 仍是 0，`reviewedBy` 里没有加过一条人名，
  `humanConfirmedAnchors` 依然如实报 0。
- 改的是「可引用」这条线画在哪，不是改数据本身。

**给你的含义**：`citable == true` 只表示「允许挂档案」，**不表示有人对它负责**。
要更严的口径，自己按 `reviewStatus` 收窄，例如只收 `auto-confirmed` + `expert-confirmed`。

### 5.4 两个容易踩的坑

**坑一：`reviewedBy` 非空 ≠ 有人看过。**
这个字段里现在的内容全是 `ai:extraction-pipeline` —— 抽取流水线自己签的。
要数「人签的字」，必须**按 `ai:` 前缀排除**，否则会虚报。

**坑二：可引用 ≠ 每个字段都好。**
`fieldIssues` 是**字段级**缺陷（证据弱、学段可疑、缺 topic、独立验证存疑、复合未拆），
和 `reviewStatus` 分开记：`reviewStatus` 回答「这条断言成不成立」，
证据写得弱是别的字段的毛病，不该让断言替它背锅。
当前 <!--N:fieldIssueAnchors-->417<!--/N--> 条存活锚点带 `fieldIssues`，其中不少是可引用的。
**展示给最终用户时请把它显示出来。**

### 5.5 成色分档（`tiers`）和「可引用」是两件事

`mappings/citable.json` 里还有一组 `tiers`：`confirmed` / `aiPassed` / `flagged` / `unseen`。
它是**成色**，用于展示（我们的图上用填充强度表示），与「能不能引用」正交。
不要把两者合成一个字段 —— 合过一次，结果是「机械可判定」和「AI 看过没挑出毛病」
在图上画得一模一样，而我们恰恰在强调这两者的区别。

---

## 6. 边（依赖关系）怎么引用

### 6.1 形状

```json
{ "anchorId": "ca_SxrKsY92", "prerequisiteId": "ca_pP52rwAH",
  "type": "component", "strength": "soft", "inInferenceGraph": true,
  "failureSignature": "他会停在第一步：……无法说出这是交响乐还是民歌……",
  "reason": "…", "evidence": [ … ], "reviewStatus": "llm-proposed" }
```

- **方向**：`anchorId` 是**被修方**（后继），`prerequisiteId` 是**前置**。
  读作「要学 `anchorId`，得先会 `prerequisiteId`」。**这个方向反过来读就全错了。**
- **边没有独立 ID**，主键是 `(anchorId, prerequisiteId)` 这一对。
  不要给边建自己的外键 —— 我们不保证边的稳定性，边可以被退休（`retired: true`，
  记录移入 `retired/edges.jsonl`）。
- 当前 <!--N:edges-->6622<!--/N--> 条边。

### 6.2 四种关系类型，只有三种进推理图

| `type` | 不具备前置时的失败表现 | 可绕过 | `inInferenceGraph` |
|---|---|---|---|
| `component` | 会做，但某一步系统性地错，错点可定位 | 否 | 是 |
| `instrument` | 能到，但绕远路、很慢、用笨办法 | 是 | 是 |
| `semantic` | 读不懂题、答非所问、或整片空白 | 否 | 是 |
| `convention` | **无可观测影响**（教材就这么排的） | — | **否** |

**要做路径推荐、解锁计算，只用 `inInferenceGraph === true` 的边**
（当前 <!--N:edgesInGraph-->4934<!--/N--> 条）。
`convention` 那 <!--N:edgesConvention-->1545<!--/N--> 条是教材编排顺序，不是能力依赖，
拿它推理会推出「不学这个就学不了那个」这种假结论。

还有 <!--N:edgesUntyped-->143<!--/N--> 条边**没有 `type`**（重标管线未覆盖到）。
它们的 `inInferenceGraph` 也缺省。**按「未定级」处理，别默认当成有效依赖。**

`failureSignature` 是这条边的判据本身：说不出具体、可观察的失败表现，就是说不出这条边。
空泛词（基础不牢／能力不足／理解不深…）会被校验器拦。**它比 `reason` 更值得你读。**

### 6.3 `strength`

`hard` = 没有它就学不了；`soft` = 影响没那么绝对。
当前 <!--N:edgesHard-->1626<!--/N--> 条 `hard`。

注意一个设计动作：MATRIX 档学科（史地生政艺外语等）的边即使模型判为 hard，
也会被压回 `soft` 并标 `strengthCappedBy: "matrix-track"` ——
那些学科的先修关系稀疏到可忽略，「不具备就卡死」这句断言不该由逐条的模型判断给出。
**这是 withhold，不是静默改写**：类型与失败表征都保留着，可查、可回滚。

### 6.4 边**基本没被复核**，这一点必须说清楚

<!--N:edgesUnreviewed-->6619<!--/N--> 条边的 `reviewStatus` 是 `llm-proposed`。
边的枚举只有四值（`llm-proposed` / `auto-confirmed` / `expert-confirmed` / `disputed`），
**没有 `ai-*` 那两档**，也就是说：`citable.json` 那条放宽线**不适用于边**。

结论：**边现在适合做探索、可视化、给人看的建议，不适合当自动决策依据。**
`counterEvidenceCount`（反例计数）恒为 0 —— 它要等 L3 聚合数据才有值，而 L3 永不进本仓库。

### 6.5 档位规则（校验器强制，你可以当不变式用）

- **`LIST` 档锚点不能当被修方**（不能有前置）—— 覆盖模型里，字 A 和字 B 之间没有先修关系。
  **例外只有一个：集合包含**（基本字表 ⊂ 常用字表这种）。走这个例外的边必须带
  `set-containment` 证据**和** `containment` 字段（包含率落盘可核），口头声明不算。
  拆原子产生的 `component` 边另有豁免（它不声称「小表 ⊂ 大表」，没有包含率可落盘）。
- **同学科内的 `LIST` 不建链。** `LIST` 当**跨学科**前置是放行的
  （「能利用网络搜集资料」可以是艺术学科的真前置）。
- **`MATRIX` 档不出现 `hard` 边**（同样豁免拆原子的 `component` 边）。
- **无环、无自环、无重复边、无学段倒挂**（前置整体晚于被修方 = 方向大概率反了）。
- **不会有边指向已弃用锚点。**

### 6.6 跨学科：先修边极少，这不是漏建

<!--N:edges-->6622<!--/N--> 条边里只有 <!--N:crossEdges-->11<!--/N--> 条跨学科
（<!--N:crossPct-->0.2<!--/N-->%）。真正的跨学科**先修**关系本来就罕见。

「能力跨界」在本项目里由**另一层**表达：横切维度 `crosscutting`（参照 NGSS 跨学科概念，
闭合词表）与 `practice`（科学与工程实践）。
当前 <!--N:crosscuttingTagged-->1902<!--/N--> 条锚点已打标。
**这两个是标签，不是边** —— 练同一个横切概念的两条锚点之间有关联，
但那关联没有方向、画不成有向边。别把它当依赖用。

---

## 7. 版本策略与变更检测

### 7.1 三个版本号，各管一件事

| | 位置 | 管什么 |
|---|---|---|
| `schemaVersion` | 每条记录上 + `manifest.schemaVersion` | **字段形状契约**。字段增删改名会动它。当前 <!--N:schemaVersion-->0.1.0<!--/N-->。 |
| `release` | `manifest.release` | 数据集发布版本。当前 <!--N:release-->0.1.0<!--/N-->。 |
| `generatedAt` | `manifest.generatedAt` | 这份快照什么时候算出来的。当前 <!--N:generatedAt-->2026-08-24<!--/N-->。 |

`schemaVersion` 在 schema 里是 `const`：**同一个快照内所有记录的 schemaVersion 必然一致**，
不会出现混版。你可以拿它做硬门禁：读到不认识的 schemaVersion 就停下来，别猜。

**不承诺**：不承诺 `schemaVersion` 遵循语义化版本的兼容性约定，
也不提供跨版本迁移脚本。变更会在 [DECISIONS.md](DECISIONS.md) 留记录。

### 7.2 校验和：`manifest.files`

`manifest.json` 的 `files` 段列出每个数据文件的 `bytes` 与 `sha256`
（当前 <!--N:manifestFiles-->107<!--/N--> 个文件）。这是判断「有没有变」的最省事的办法：

```js
const m = await (await fetch('.../data/manifest.json')).json();
for (const [path, { sha256 }] of Object.entries(m.files)) {
  if (sha256 !== myCache[path]) refetch(path);   // 只重取变了的文件
}
```

### 7.3 怎么判断两次快照之间**变了什么**

按这个顺序，别跳步：

1. **比 `release` / `generatedAt` / `schemaVersion`。**
   schemaVersion 变了 → 先读变更记录，别急着 diff 数据。
2. **比 `files[*].sha256`**，挑出变化的文件重取。
   注意文件可能整个消失或新增（内部切分会调整），这本身不代表数据没了。
3. **按 `id` 做全局比对，不要按文件比对。**（ID 的文件归属不稳定，见 §3。）
   把两次快照的全部锚点合成两张 `id → anchor` 表，然后：

   | 情形 | 含义 | 你该做什么 |
   |---|---|---|
   | 新 ID 出现 | 新增锚点 | 可选：看要不要标注 |
   | `deprecated` 由 false → true | **弃用** | 走 [§4.3](#43-引用了被弃用锚点怎么办) |
   | `reviewStatus` 变了 | 复核状态变化 | 重新判断 citable；可能是升级也可能是降级 |
   | `statement` 变了 | 表述被打磨 | 看 `statementBefore`，判断含义有没有变；含义变了本该是弃用+新 ID，如果不是，**这是我们的 bug，请报** |
   | ID 从数据里消失 | 违约 | 报 issue |

4. **边**：主键是 `(anchorId, prerequisiteId)`，比对方式同理。
   `retired: true` 的边视同不存在（但记录留着，可查为什么退休）。

### 7.4 不承诺历史快照

线上是覆盖式部署，`/data/` 永远只有最新一份，**我们不提供历史版本的归档下载**。
要留档，请自己在取数当天保存 `manifest.json`（它带全部 sha256），
或者直接 `git clone` 仓库并记下 commit。

---

## 8. 许可边界

以下表格照抄自 [README.md](README.md) 与 [PROVENANCE.md](PROVENANCE.md)，**逐字不改**：

| 层 | 许可 |
|---|---|
| L0 锚点 + 依赖图、L1 映射（数据库结构与关系） | ODbL 1.0 |
| 本项目撰写的文本（statement / reason / evidence / assessment） | CC BY-SA 4.0 |
| `provenance.srcText` 句子级课标引文 | **不属于本项目**，权利归教育部。按合理引用附带，可用一键剥离开关剥离 |
| 课标 PDF 原件、诗文正文 | 不发 |
| L2 编排层、L3 档案 | 不在本仓库 |

补充自 [LICENSE](LICENSE)：**代码（`scripts/` 与 `tools/`）：MIT。**

### 关于 `provenance.srcText`

<!--N:srcTextAnchors-->2816<!--/N--> 条存活锚点带句子级课标引文，中位数 33 字、最长 183 字，
单句、不连段。项目对它的表述（照抄 PROVENANCE.md）：

> **我们的判断**：单句、非连续、用于标注与核查目的的引用，按合理引用处理。
> **这是我们自己的判断，不是法律意见，我们也不是权利人。**

需要不含引文的数据副本时：`node scripts/strip-srctext.mjs --dir dist/data`
一条命令剥掉全部引文，只留页码，可核查性不损失。

### 你作为引用方的义务

**待法律意见。** ODbL 1.0 与 CC BY-SA 4.0 对下游的署名、共享相同方式、
以及「衍生数据库 / 生产作品」的界定，属于法律理解，**本项目不自行撰写解释**。
在拿到法律意见之前：

- 许可全文在 <https://opendatacommons.org/licenses/odbl/1-0/> 与
  <https://creativecommons.org/licenses/by-sa/4.0/>，以全文为准。
- 学术或产品中引用本数据集，请按 [CITATION.cff](CITATION.cff) 署名。
- 有商业使用、再分发、闭源产品内嵌等具体场景的疑问，请直接来问（[§10](#10-要新锚点或发现错误)），
  我们不会用一句想当然的话把你带沟里。

锚点详情页规划里的 `license_note` 字段（见 `specs/003-anchor-detail.md`）**故意留空**，
理由同上：等法律意见，不自己编。

---

## 9. 我们**不承诺**什么

这一节和上面所有承诺一样重要。按重要性排：

1. **没有教师签字。** <!--N:humanConfirmed-->0<!--/N--> 条。
   全库 <!--N:liveAnchors-->2971<!--/N--> 条断言没有一条经过一线教师复核。
2. **没有判定方法。** `assessmentSpec`（题型、判定规范、通过标准、衰减提示）
   当前 <!--N:assessmentSpecAnchors-->0<!--/N--> 条有值。
   **空着是对的** —— 用模型批量编样题会污染底座且无法追溯。
   底座能告诉你「这是一条什么能力」，**不能告诉你「怎么判他会不会」**。
   （`assessment` 字段有值，但那是给家长/老师看的自然语言问句，不是判定规范，
   而且含 `{{name}}` 占位符，展示前必须替换或剥离。）
3. **没有掌握判据、变式空间、典型错误。** `mastery` / `variations` / `misconceptions`
   三样都没有（分析见 [docs/atom.md](docs/atom.md)）。
   所以「做对几道算会、隔一个月还算不算会」这类问题，底座答不了。
4. **没有年级精度。** `stageHint: {min, max}` 是**学段区间提示，非权威**。
   真正的年级来自 L2 编排层（教材版本×年级），**L2 不在本仓库**。
   同一锚点在不同教材落在不同年级。**不要拿 stageHint 当年级判据给孩子分级。**
   另外：学段的年级区间**按学科取**（艺术是 1-2／3-5／6-7／8-9，与别科不同）。
5. **没有教材对齐。** 未与任何出版社教材对齐，也不打算接来源不清的教材库
   （评估与理由见 PROVENANCE.md「未做的事」）。
6. **边基本未经复核**，见 [§6.4](#64-边基本没被复核这一点必须说清楚)。
7. **覆盖不均。** 各学科条数差距很大（`manifest.counts.byDiscipline` 自己看，
   别信任何文档里手打的分布）。有的学科只有个位数锚点。
   **锚点少不等于那个学科简单，等于我们还没做到那里。**
8. **L3 档案永不进本仓库。** 未成年人个人信息属敏感个人信息。
   本仓库不含、也永不接收任何学生数据；`schema/assertion.schema.json` 只定义接口契约，
   `.gitignore` 硬性拦截任何断言数据。
   **不要给我们发学生数据**，包括「脱敏样例」。
9. **禁止字段永远不会有。** `difficulty`（难度）、`averageMasteryAge`（平均掌握年龄）、
   `peerPercentile`（同龄百分位）三个字段被 schema 的 `forbidden` 关键字禁止、
   由校验器执行。前两个在无真实作答数据前是编的，第三个是横向比较的入口，进来了拿不掉。
   **它们的缺席是设计结论，不是没来得及做。** 别问什么时候加，答案是不加。
10. **没有 API、没有后端、没有 SLA。** 纯静态站点 + 静态文件，没有查询接口、
    没有可用性承诺。要稳定就自己缓存一份。

---

## 10. 要新锚点，或发现错误

### 铁律：不许在自己库里造知识点

> **L4 的任何应用不得自己定义知识点，只能引用 L0 的 ID。**

发现底座里没有你要的锚点 → 走申请通道，**不要在自己库里造一个**。
这条一破，底座立刻退化成又一个数据集，所有产品的档案重新变成互相读不懂的孤岛 ——
你自己的档案也会变成孤岛。

### 通道

| 你要做的事 | 怎么做 |
|---|---|
| 申请新锚点 | 提 issue / PR 到 `review-queue/`。请给出：拟议断言（能回答「会/不会」的那种）、学科、课标出处（哪份文件哪一页），三样缺一不可。 |
| 报错误锚点 | 提 issue，附 `ca_` ID。每条锚点有独立链接 `k12.yongle.school/a/<id>/`，直接把链接发来最省事。 |
| 报错误的边 | 附 `(anchorId, prerequisiteId)` 这一对，并说明「不具备前置的孩子在后继上失败长什么样」—— 说不出来，那条边本来就该降级。 |
| 学科级问题 | 见 [OWNERS.md](OWNERS.md)，每个学科分片有主编（多数**待认领**）。 |

仓库地址在 [CITATION.cff](CITATION.cff) 里（`repository-code`）。

### 异议目前是什么状态

锚点 schema 里有 `objections`（公开异议）字段，**只落库，不做工作流** ——
谁看、怎么处理、多久回应，**现在都没有定**。计数会在详情页公开可见。
把这个说清楚，好过让你以为提了就会有人处理。

---

## 11. 这份文件本身怎么变

- 本文里带 `<!--N:…-->` 标记的数字**一个都不是手打的**，由 `scripts/sync-docs.mjs`
  从 `manifest.json` 现读、CI 核对不一致就失败。（接线状态见文件开头的施工牌。）
  这个机制的由来：README 曾经在同一段里第 13 行写「138 条可用」、第 16 行写「依然是 0」，
  两个都错、而且互相矛盾，挂在首页好几轮没人发现。
  **承诺要么可执行，要么会腐烂。**
- §3 / §4 / §7 里的「承诺」是对外契约。**要改，必须先在 [DECISIONS.md](DECISIONS.md)
  留一条带理由和备选方案的记录**，并在本文标出变更日期。
- 散文式的断言（不带数字标记的那些句子）**没有任何机器在核对**，只能靠人回头发现。
  在本仓库这已经发生过：PROVENANCE 里「高中的先修边为空」变成假话之后，
  是人回头看才发现的。看到本文里有和数据对不上的说法，**请报**，那是 bug 不是措辞问题。
