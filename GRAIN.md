# 粒度口径

一句话：**锚点是坐标系的刻度，不是教学单元。**

不是一节课，不是一道题，不是一个知识点。它是「课标在这个学段对这个方面的一句要求」。

## 事实

| 一条锚点覆盖 | 占比 |
|---|---|
| 1 个年级 | <!--N:grainSpan1-->4.9<!--/N-->% |
| 3 个年级 | <!--N:grainSpan3-->67.6<!--/N-->% |
| 4 个年级及以上 | <!--N:grainSpanWide-->10.6<!--/N-->% |

三分之二的锚点覆盖一整个学段。这不是抽取做坏了 —— **课标本来就按学段写**，
它要管的是「这三年里学生要达到什么」，不是「这节课要教什么」。

## 这意味着什么：一个真例子

拿一节真课去调用底座 —— 三位数连续退位减法，教学目标很窄：

```
查询   本节课学习用竖式计算三位数减法。重点讲退位怎么发生：
       个位 0 减 8 不够减，要向十位借，十位是 0 又要向百位借。

第 1 名  能计算两位数和三位数的加减法          ca_2cp77aYv  课标 p26  G1–G2
```

**这个结果是对的。** 问题在于它对得没有用 —— 它覆盖两年，包含两位数、三位数、
加法、减法。调用方拿到它之后知道了「这属于加减法领域」，而那他本来就知道。

一节 40 分钟的课教的「连续退位」，在课标的粒度里根本不是一个单独的东西。

## 所以，映射的正确用法

**✓ 定位。** 「这段内容落在坐标系的哪个位置」—— 学科、学段、能力方向。
**✓ 溯源。** 「这个要求出自教育部哪份文件哪一页」。
**✓ 找前置。** 「要够到这个位置，前面得先站住哪几个位置」。

**✗ 当标签用。** 「这道题 = ca_2cp77aYv」—— 一条锚点底下有上百道题，这个等号不成立。
**✗ 当进度用。** 「他做对了这道题 ⇒ 他会了 ca_2cp77aYv」—— 那条锚点要两年才走完。
**✗ 当教案用。** 锚点不告诉你怎么教、教多久、分几课时。课标里没有这些。

## 接口怎么表达这件事

不靠文档，靠**每次返回都带着**。`grainWarning` 是 `mappings/grain.json` 定义的分档：

```
1 个年级      无警告
2–3 个年级    这条覆盖 N 个年级，是学段级的坐标刻度，不是某一节课的目标。
4 个年级以上  这条覆盖 N 个年级、跨越学段，只能用来定位大方向，
              不能用来判断某个孩子此刻会不会。
学段未知      这条没有可用的学段范围，无法判断粒度。
```

**粒度只有这一处定义。** 曾经打算在锚点上存一个 `span` 字段，后来否掉了 ——
存字段等于给粒度立第二个定义，它一定会和 `stageHint` 漂移。
这个项目专门有一道 `no-dup-defs` 拦的就是这种事（「可引用」的定义一度散在 8 个文件里、
3 个不同的值）。所以：**不存，算**。Node 走 `scripts/lib/grain.mjs`，
Python 走 `tools/grain.py`，阈值两边读同一份 JSON。

## L0.5：留了位置，但现在不许建

五层架构（L0 锚点 / L1 课标映射 / L2 教材 / L3 个人档案 / L4 应用）里，
L0 和 L1 之间**一直留着一层没建** —— 细到「可教可判定」的教学切分。
它是唯一能同时满足「可被细粒度调用」和「每条都能翻回教育部文件某一页」的解：
L0 一个字不动，L0.5 明确标注为机器推导、非课标原文、单独的 ID 空间、单独的可信度。

**建它的前提是先有一个真实调用方撞到粒度墙。**

现在建，我只能按自己的想象切，然后重复这个项目已经犯过两次的错 ——
在没有需求证据的情况下造供给（2,969 个详情页，八天里三个被打开过）。
L0.5 的形状必须由真实的粒度抱怨来定，不是由猜。

在那之前，粒度问题的处理方式就是上面那条：**摆在调用现场，不藏起来。**
