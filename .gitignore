node_modules/
.DS_Store
*.log

# L3 档案数据永远不进仓库。schema/assertion.schema.json 只定义接口契约，
# 实际断言数据必须留在私有/校内存储 —— 未成年人信息属敏感个人信息。
profiles/
assertions/
*.assertions.jsonl

# 模型响应缓存（24MB，可按 prompt 哈希重建，不该进版本库）
tools/.cache*/

# 课标原件：教育部 PDF 共 298MB，且我们只发自编码不发原文，一律不入库。
# 用 scripts/fetch-standards.sh 现取。
sources/

# 中间产物整目录不入库（19MB，全部可由 tools/ 流水线重算）。
#
# 原先这里是**逐个文件**列黑名单，漏了 68 个 —— 而那 68 个里带着 8,945 处
# `provenance.srcText`（课标原文引文），包括已弃用和已驳回的候选。
# 这些文件没有任何发布用途，是纯误伤：白名单式忽略必然漏，改成整目录。
# CI 与 deploy 都不读 tools/out，只有 Python 流水线各段之间传递，可重算。
tools/out/

# 对账：日志是原始记录，含完整会话内容，不进仓库
日志/
