# 数据来源与权利

## 这一页曾经写错过

2026-08-17 之前，本页写着「**课标原文一律不发**」。那句话是假的：
仓库里 963 条存活锚点的 `provenance.srcText` 装的就是课标原文句子，
`dist/` 一并发到了公网，`tools/out/` 的中间产物里还有 8,945 处。

留着这段记录，是因为**错的不是那句话，是它只是一句话**。
现在「剥离原文」是一个可执行的开关（`scripts/strip-srctext.mjs`），
本页的数字由 `scripts/sync-docs.mjs` 从 `manifest.json` 生成、CI 核对不一致就失败。
承诺要么可执行、要么会腐烂。

## 我们实际发了什么

| 来源 | 权利人 | 发 | 不发 |
|---|---|---|---|
| 《义务教育课程方案和课程标准（2022年版）》 | 中华人民共和国教育部 | 自编码 ID、**自己改写的能力断言**、页码、**句子级原文引文**（见下） | 整段/整节/整章原文、PDF 原件 |
| 《普通高中课程方案和课程标准（2017年版2020年修订）》 | 同上 | 同上 | 同上 |
| 语文课标 附录1 优秀诗文背诵推荐篇目 | 同上 | 篇目名、作者、学段（事实性清单） | **诗文正文** |
| 语文课标 附录4/5 基本字表、常用字表 | 同上 | 字表条目与官方编号（事实性清单） | — |
| 英语课标 附录 词汇表 / 不规则动词表 / 语法项目表 | 同上 | 词条与编号（事实性清单） | — |

### 关于句子级原文引文（`provenance.srcText`）

- **规模**：<!--N:srcTextAnchors-->2816<!--/N--> 条存活锚点带引文，中位数 33 字、最长 183 字。单句，不连段。
- **用途**：三处都是承重的，不是留着好看 ——
  1. **溯源**：改写得对不对，看的人得能对着原文判断
  2. **接地校验**：自动检查断言有没有偏离出处
  3. **模型污染闸**：同一句原文出现在多个学科下 = 模型在别科页面吐了记忆里的原文。
     这道闸抓出过「知道甲骨文是已知最早的汉字」同时挂在 5 个学科下。**没有原文它直接失效。**
- **我们的判断**：单句、非连续、用于标注与核查目的的引用，按合理引用处理。
  **这是我们自己的判断，不是法律意见，我们也不是权利人。**
- **权利人若提出要求**：`node scripts/strip-srctext.mjs --dir dist/data` 一条命令剥掉发布产物里的
  全部引文，只留页码，可核查性不损失。这个开关随时可跑，不需要改代码。

### 已从仓库移出的

`tools/out/`（19MB，85 个中间产物文件）**已整目录移出版本管理，并改写历史清除**。
里面是流水线各段之间的传递文件，含已弃用与已驳回的候选，全部可重算，没有发布用途。

2026-08-17 用 `git filter-repo --path tools/out --invert-paths` 改写了全部 36 个提交，
force-push 到 main。改写前做过 dry-run 路径比对：删除的 68 个路径全部属于 `tools/out/`，
无误伤、无新增。本地历史里 `tools/out` 残留对象为 **0**。

⚠️ **但 force-push 不等于删除 —— 这一步失败了。** 实测：改写后旧 commit `eb2f040`
及其下的 blob 仍能从 GitHub 完整取回（2.25MB，`raw.githubusercontent.com` 返回 200）。
GitHub 在 force-push 后**不会自动回收悬空对象**，任何拿到旧 SHA 的人照样读得到。

所以又做了一步：**删库重建**（本仓库当时 0 fork / 0 star / 0 issue / 0 PR，建库仅两天，
代价只有 clone 流量统计和 Actions 运行历史）。重建后复验：旧 commit、旧 blob、
raw 路径**三者全部 404**，这次是真的没了。

**留下这段失败记录，是因为它本身就是本文件存在的理由：**
改写历史是「做了动作」，旧对象仍可达是「没达到效果」。上一版 PROVENANCE 犯的
正是把前者写成后者的毛病 —— 那就不能在修它的时候再犯一次。

## 抽取方式

**两套课标的抽取路径完全不同，别套用。**

| | 义务教育（2022） | 普通高中（2017/2020修订） |
|---|---|---|
| 份数 / 页数 | 15 份 · 1,594 页 | **21 份 · 2,276 页** |
| 体积 | 298MB（150 DPI 扫描） | **52MB** |
| 文字层 | **零**，`pdftotext` 完全无效 | **全部有**，逐份抽样均可提取 |
| 抽取方式 | 多模态模型逐页识读 | **直接 pypdf 提取** |
| 主要风险 | 识读幻觉、串页、模型污染 | 版面切分（分栏/页眉混入） |
| 取源脚本 | `scripts/fetch-standards.sh`（中国政府网） | `scripts/fetch-standards-gaozhong.sh`（人教社） |

义务教育那套的坑记在 [docs/EXTRACTION.md](docs/EXTRACTION.md)。高中这套**不要**走
`tools/extract_pages.py` —— 那是为图像页写的，对有文字层的 PDF 是白花钱且凭空引入幻觉。

每条锚点都带 `provenance.srcPage`，可翻回课标原页核对。

课标 PDF 原件（义务教育 15 份 298MB + 高中 21 份 52MB）**均不随仓库分发**，
用上表两个脚本现取。

## 复核状态（真实数字）

<!--N:reviewTable-->| 状态 | 条数 | 能否被 L3 档案引用 |
|---|---:|---|
| `auto-confirmed` | 146 | 能 |
| `ai-adjudicated` | 242 | 能（AI 裁定，**待人工异议**） |
| `expert-confirmed` | 0 | 能 |
| `ai-reviewed` | 2201 | 能（2026-08-20 起 —— **AI 看过、没挑出毛病**，不是教师签字） |
| `disputed` | 20 | **不能** —— AI 复核挑出了具体问题 |
| `llm-proposed` | 362 | **不能** —— 没有任何东西看过一眼 |
| **存活合计** | **2971** | 其中 **2589** 可用 |<!--/N-->

**教师签字数：<!--N:humanConfirmed-->0<!--/N-->。**
`reviewedBy` 字段里有 <!--N:aiSigned-->355<!--/N--> 条非空，但内容全是 `ai:extraction-pipeline` ——
那是抽取流水线自己签的，不是人。字段名容易误读，在此说明。

## 未做的事

- **未与任何出版社教材对齐。** 教材是课标的产生品，各省市版本不同；
  课标才是所有教材共同的稳定基准。教材编排层（L2）不在本仓库。

  **也不打算用现成的教材仓库补这一层。** 2026-08-19 评估过
  [TapXWorld/ChinaTextbook](https://github.com/TapXWorld/ChinaTextbook)
  （41GB、8 万星、18k fork，小初高大学教材 PDF，仅小学数学就有 8 个版本）。
  内容上它正好补我们缺的那块 —— 有了它，档案才答得出「他手上这本书下一课学什么」。
  但**不接**，三个理由：

  1. **无 LICENSE**，权利人是人民教育出版社等。该仓库自己的叙事是
     「这些本该免费」—— 那是道义主张，不是授权。
  2. **我们已经为版权做过架构决策，不能自己推翻。** 连课标原文引文都逐条
     说明了合理引用边界、还做了一键剥离开关；那么严谨地处理教育部的公开文件，
     反手接一个来源不清的 41GB 教材库，逻辑上站不住。
  3. **底座的护城河是「可信」不是「全」。** 唯一的稀缺资产是「每条都能翻回
     教育部文件某一页」，接进来源不清的内容那条链就断了。

  唯一可用的是它的**目录结构**（哪些科、哪些省、哪些出版社在用）——
  那是事实性清单不是作品。将来真做 L2 映射，正路是国家中小学智慧教育平台的
  官方电子教材，或直接与出版社谈。
- **未经任何教师复核。** 上表 <!--N:usable-->2589<!--/N--> 条「可用」里：
  <!--N:autoConfirmed-->146<!--/N--> 条机械可判定（字表词表这类数得清的东西）、
  <!--N:aiAdjudicated-->242<!--/N--> 条 AI 裁定待异议、
  <!--N:aiReviewedCount-->2201<!--/N--> 条 AI 复核过且未被挑出问题。
  **没有一条有教师签字。**

  2026-08-20 起 `ai-reviewed` 计入可用（户主明示）。教师签字通道本来就还没开，
  继续把这一千多条标成「不可引用」等于让底座对外没有可用规模。
  放宽的是「AI 看过、没挑出毛病」这一档，`disputed` 与 `llm-proposed` 仍然不可引用。
  **没有伪造任何教师签字**，`humanConfirmedAnchors` 依然如实报
  <!--N:humanConfirmed2-->0<!--/N-->。定义见 `mappings/citable.json`。
- ~~**高中的先修边为空。**~~ **已不成立**（2026-08-20）。高中边已建并完成语义重标，
  全库 3,069 条边分成 component / instrument / semantic / convention 四类，
  每条带失败表征。见 [reports/edge-retag-summary.md](reports/edge-retag-summary.md)。
  **留着这条划掉的旧文，是因为它正是「文档承诺会腐烂」的样本** ——
  数字有 `sync-docs` 现读核对，而这种散文式的断言没有，只能靠人回头发现。
- **不含任何个人数据。** L3 档案层永不进仓库，见 README「隐私」一节。

## 许可

| 层 | 许可 |
|---|---|
| L0 锚点 + 依赖图、L1 映射（数据库结构与关系） | ODbL 1.0 |
| 本项目撰写的文本（statement / reason / evidence / assessment） | CC BY-SA 4.0 |
| `provenance.srcText` 句子级课标引文 | **不属于本项目**，权利归教育部。按合理引用附带，可用上述开关剥离 |
| 课标 PDF 原件、诗文正文 | 不发 |
| L2 编排层、L3 档案 | 不在本仓库 |
