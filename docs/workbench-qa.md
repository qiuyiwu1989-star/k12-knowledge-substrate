# 005 验证记录

## 自动检查（本地，2026-09-05）
- `npm run workbench-test`：7 组通过。范围与分页、任务隔离、输入边界、出处防伪、确认条件、版本冲突、SQLite 重启、会话隔离、真实 HTTP、真实 MCP stdio、底座文件不变。
- `npm run check`：通过，包含原有 19 项 MCP 自测。首次因 manifest 当天时间与 SPEC.md 标记日期不同中断，运行原有 sync-docs 同步两处日期后通过，未修改任何课标节点。
- `node --check workbench/public/app.js`、`bash -n deploy/workbench-install.sh` 通过。
- `npm install --prefix workbench --ignore-scripts`：14 个依赖，audit 0 个漏洞。
- evaluation.xml 的 10 个预期 ID 均核对存在、页码和年级；以对应任务文本运行召回均进入前 12。它是工具回归题，不是教师金标准；尚未运行外部模型代理评测。

## 真实浏览器（Codex 浏览器）
- 桌面 1280 宽：初始状态、材料实验示例、3 项任务真实召回、18 条待确认显示正常。
- 空关系/理由点击确认，出现明确错误；选择关系、填写原文对应理由后确认成功。
- 保存成功，修订 1；手动检索「温度」补充能力，排除后保存修订 2。
- 覆盖图仅 1 条已确认关系；点击任务 2 节点跳回任务 2。
- 手机 390×844：输入、项目对话框和载入正常；已保存确认/排除状态恢复。document scrollWidth=clientWidth=390，页面无横向溢出，关系图内部可横滚。
- 测试后恢复浏览器默认视口。
- 补测 ArrowRight 选中任务 2、End 选中任务 3；修改应用名称后显示结果过期，保存/导出/确认均禁用。

## 部署验证（2026-09-06，Asia/Seoul）
- 运行提交 b490caa，GitHub Actions 33975681558 两个检查成功。
- 生产 HTTPS 真实召回命中 ca_VKU9hc8E；规范化确认项目保存 PostgreSQL 并原样读回，更新至修订 2。
- 新会话项目列表为空，读取其他会话项目返回 404；使用旧修订更新返回 409。
- 旧首页含工作台入口，/zhishi/ 返回 200；本机公网 curl 与服务器 HTTPS 健康检查通过。
- 首次 nginx reload 后旧 worker 返回 404，自动恢复；添加健康检查重试后上线成功。备份和版本详见 STATE.md。
- 线上浏览器导航两次超时；实际界面验证在本地同版本完成，生产接口通过 HTTPS 验证。
- 部署冒烟脚本先修正了两处测试假设：保存前会规范化召回说明，项目列表格式为 {items:[]}。修正后全流程通过，应用代码无需变更。

## 产品边界
字面召回存在语义误召回，例如实验任务可能召回只共享「观察对比」的植物条目。已明确标候选并要求人工确认，不以技术自测冒充教育有效性验证。
