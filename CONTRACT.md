# 数据契约

这份文件管的是**数据**，不是网站。网站是文档，可以随时改版；下面这些承诺是给
「存了锚点 ID 之后指望它一年后还指同一个东西」的调用方的。

当前版本 <!--N:contractVersion-->1.0<!--/N--> ·
锚点 <!--N:anchorsAll-->3997<!--/N--> 条（含弃用与归档）·
边 <!--N:edges-->6618<!--/N--> 条

## 规矩：每条承诺旁边必须站着一道闸

写在文档里的承诺会腐烂。这个项目已经验证过三次 —— `split_reqs` 的注释和切分条件
说的不是一件事、`evidenceSource` 引用了未定义的变量、`gaozhong_commit` 的注释写着
「追加」而代码是 `open('w')`（静默冲掉 29 条）。

所以这份契约的编写规则是：**没有闸守着的条款，不许写进来。**
下面每一条后面括号里就是守它的那道闸，`npm run check` 一次全跑。

---

## 一 · ID

**ID 永不消失。** 一条锚点可以标 `deprecated`（带 `supersededBy` 或 `dropReason`），
但它的记录必须还在仓库里 —— 换个文件放没关系（`retired/` 归档也算），
只要还解析得到。（`no-shrink`）

**ID 永不改指向。** 同一个 ID 永远指向课标同一处出处。判据是
`(srcSubject, srcPage)` 而不是断言原文 —— 原文会合法地改（我们修过 CID 字体抽出来的
乱码），改字不算换锚点，换锚点一定换出处。台账在 `ledger/ids.jsonl`，
目前 <!--N:ledgerIds-->3997<!--/N--> 条，只增不改。（`no-id-reuse`）

**ID 永不复用。** 弃用的 ID 也不许被新记录占用。（`no-id-reuse` + `validate`）

## 二 · 版本

版本号形如 `MAJOR.MINOR`。变更分四类，由机器分类、机器核对：

| 类别 | 什么算 | 要求 |
|---|---|---|
| **breaking** | ID 从仓库消失 · 学科变 · 出处变 | 必须进 major，且逐条写进 `CHANGELOG.md` |
| **deprecating** | 某条被标弃用 | 必须进 minor，且逐条写进 `CHANGELOG.md` |
| **revising** | 断言原文改了 · 学段范围改了 | 必须进 minor |
| **additive** | 新增锚点 · 新增边 | 进 minor |

`reviewStatus` 升档**不属于任何一类** —— 复核变好不该惊动任何人。

破坏与弃用为什么还要逐条写进 CHANGELOG：版本号是给机器看的，CHANGELOG 是给人看的。
要求逐个 ID 写进去，是**逼一个人真的去看每一条**。破坏性变更少到能一条条看；
多到看不完的时候，本身就说明这次不该发。（`version-diff`）

**每个发布版本永久可取。** 指纹存在 `releases/<版本>.json`（不可变，已发布的不许覆盖），
全量数据发到 `/data/v/<版本>/`。调用方 pin 到哪个版本，那个版本就一直在。

## 三 · 溯源

每条断言必须能翻回教育部课标文件的某一页 —— `provenance.srcPage` 必填。
（`validate`）

**已知盲区，两个数，量的不是一回事：**

- <!--N:unknownProvenance-->305<!--/N--> 条锚点**出处不完整**（缺 `srcText` 或 `srcPage`）——
  影响的是「能不能翻回原文核对」。
- <!--N:blindIds-->150<!--/N--> 条记录缺 `srcSubject`/`srcPage`，**`no-id-reuse` 判不了它们的指向**。
  拿未知去比对，会把「一直未知」误报成「被挪动」，所以它们被排除在判定之外。
  这一档扫 `anchors` + `retired`，基数和上面那个不一样。

两个数字都应当只减不增。

## 四 · 只读

**映射结果不写回底座。** 别人拿底座去标注自己的内容，那个标注是别人的判断；
混进来，底座就不再是「每条都能翻回教育部文件某一页」，而那是它唯一的护城河。

登记为只读的工具里**一个写原语都不许有**。要输出走 stdout；要落盘的
（比如调用计数）拆到单独模块。（`no-writeback`）

判据故意放得很粗 —— 不去分析「这个 write 的目标是不是 anchors/」，
因为路径可以拼、可以传参、可以从环境变量来，静态分析追不动，
**追不动的判据等于没有判据**。

## 五 · 可判定

每条断言必须能回答「这个孩子会不会」。不可判定的不许入库。
（`selftest` · `decidability`）

---

## 这份契约不承诺的

诚实地列在这里，比让调用方自己撞上要好：

- **没有一条锚点经过教师签字。** 教师签字数 <!--N:humanConfirmed-->0<!--/N-->。
  `reviewStatus` 里除了 `expert-confirmed` 之外的所有档位都是机器判的，
  「可引用」的准确含义是**AI 看过、没挑出毛病**。
  对外接口只暴露 `verifiedBy: "ai" | "human" | null`，不暴露四档 —— 那是虚假精度。
- **覆盖率未知。** 我们知道抽出了多少条，不知道课标里应该有多少条。
  已知的页码空档见 `reports/coverage.md`。
- **粒度是课标的粒度，不是教学的粒度。** 见 `GRAIN.md`。
  <!--N:grainSpan3-->67.6<!--/N-->% 的锚点跨 3 个年级，
  只有 <!--N:grainSpan1-->4.9<!--/N-->% 是 1 年精度。
  接口每次返回都会附带粒度警告。
- **边不是教学顺序。** 边表达的是依赖，`convention` 类型的边（教材惯例）
  已被排除在推理图之外。

## 怎么 pin

**主路径是 git tag。**

```
git checkout v1.0
```

数据本来就在仓库里，切过去是瞬时的、可靠的、可校验的（tag 是带注释的，
里面写着那个版本的锚点数与边数）。指纹在 `releases/<版本>.json`，
`version-diff` 拿它判断两个版本之间变了什么。

**HTTP 快照是便利品，不是主路径。**

```
https://k12.yongle.school/data/v/<版本>.tgz     # 4.4MB
https://k12.yongle.school/data/v/index.json     # 有哪些版本
```

⚠️ **实测这条路不好走。** 2026-08-24 测得站点出网 **6 KB/s（48 kbps）**，
4.4MB 要 12 分钟，四次尝试全部失败（TLS 握手错 35 / HTTP2 帧错 16 /
超时 28 / 连接失败 000）。TLS 握手本身在 1.9–17.9 秒之间抖。

服务器本身没问题：负载 0.95，本机回环取同一个文件 213 MB/s。
**瓶颈只在出网那一段，不是这个项目能修的。**

服务端支持 `Accept-Ranges`，所以要走这条路就带续传：

```
curl -C - --http1.1 -o k12-1.0.tgz https://k12.yongle.school/data/v/1.0.tgz
```

MCP server 直接读工作区的数据，不走网络 —— 它不受这个问题影响。
